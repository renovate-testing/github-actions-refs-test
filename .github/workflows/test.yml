name: Test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # Standard semver tag
      - uses: actions/checkout@v4

      # Feature-oriented tag (non-semver)
      - uses: taiki-e/install-action@cargo-llvm-cov

      # Feature tag with digest pinning
      - uses: taiki-e/install-action@5ade09c4a4c1ee79e3a5fb335ebe1bab295d22eb # tag=just

      # Branch reference
      - uses: actions/checkout@main

      # Branch with digest pinning
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # main

      - run: echo "All action types work!"
